version: '3.8'

services:
  mashed:
    build: .
    container_name: mashed-game
    ports:
      - "80:3001"
    volumes:
      - uploads:/app/uploads
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - PORT=3001
    restart: always

volumes:
  uploads:
